import EditUserPage from '../src/pages/EditUserPage';
import React from 'react';
import {shallow, mount} from 'enzyme';
import MuiThemeProvider from 'material-ui/styles/MuiThemeProvider';
import dbapi from '../src/apirequests/dbapi';
import PATHS from '../src/global/paths';

test('Update Data populates data properly');

test('Update Data catches error response');

describe('Tests that have data presetup', () => {
    
    beforeEach(() => {
        // populate data
    });
    
    test('Editing a user works')
    
    test('Deleteing a user works');
    
    test('Filtering works properly', () => {
        // check to see that only filtered values are displayed
        // check to make sure that clicking and then deleteing user after filtering deletes the proper one
    });
});
